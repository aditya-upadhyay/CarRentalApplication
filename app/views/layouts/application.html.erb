<!DOCTYPE html>
<html>
  <head>
    <title>CarRentalApp</title>
    <%= csrf_meta_tags %>
    <p class="alert"><%= alert %></p>
    <p class="navbar-text pull-right">
      <% if customer_signed_in? %>
          <div id ="Welcome">Welcome, <%= current_customer.name %></div>
          <%= link_to 'Edit profile', edit_customer_path(current_customer), :class => 'navbar-link' %>
          <%= link_to 'Back to home page', home_path, :class => 'navbar-link'  %>
          <%= link_to 'Logout', destroy_customer_session_path, method: :delete, :class => 'navbar-link'  %>
      <% end %>
    </p>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track'=>true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
